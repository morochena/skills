interface:
  display_name: "Shape Work"
  short_description: "Resolve decisions until work is ready"
  default_prompt: "Use $shape-work to resolve material decisions until this work is ready to plan or build."
policy:
  allow_implicit_invocation: false
