interface:
  display_name: "Shape Work"
  short_description: "Resolve decisions through talk or working probes"
  default_prompt: "Use $shape-work to resolve material decisions through grounded conversation or a cheap working probe."
policy:
  allow_implicit_invocation: false
