interface:
  display_name: "Review Work"
  short_description: "Run a findings-first implementation review"
  default_prompt: "Use $review-work to run a findings-first review of this implementation."
policy:
  allow_implicit_invocation: false
