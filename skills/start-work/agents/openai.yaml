interface:
  display_name: "Start Work"
  short_description: "Route by uncertainty, risk, and coordination"
  default_prompt: "Use $start-work to choose the smallest evidence-producing workflow for this task."
policy:
  allow_implicit_invocation: false
