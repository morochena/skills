interface:
  display_name: "Brainblast"
  short_description: "Diverge, then converge on product ideas"
  default_prompt: "Use $brainblast to diverge through grounded lenses, then converge on promising directions."
policy:
  allow_implicit_invocation: false
