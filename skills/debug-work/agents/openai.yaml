interface:
  display_name: "Debug Work"
  short_description: "Run a red-to-green diagnosis loop"
  default_prompt: "Use $debug-work to establish a red signal, diagnose it, and turn it green."
policy:
  allow_implicit_invocation: false
