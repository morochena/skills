interface:
  display_name: "Do Work"
  short_description: "Coordinate implementation from a plan"
  default_prompt: "Use $do-work to implement this plan with coordinated worker agents where useful."
policy:
  allow_implicit_invocation: false
