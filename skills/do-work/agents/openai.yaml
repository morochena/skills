interface:
  display_name: "Do Work"
  short_description: "Implement in small verifiable slices"
  default_prompt: "Use $do-work to implement this settled intent through small working slices and risk-matched verification."
policy:
  allow_implicit_invocation: false
