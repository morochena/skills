interface:
  display_name: "Do Work"
  short_description: "Coordinate and integrate implementation"
  default_prompt: "Use $do-work to coordinate, integrate, and verify this settled implementation plan."
policy:
  allow_implicit_invocation: false
