interface:
  display_name: "Plan Work"
  short_description: "Plan coordination from established evidence"
  default_prompt: "Use $plan-work to coordinate shared boundaries, blocking edges, ownership, and verification from established evidence."
policy:
  allow_implicit_invocation: false
