interface:
  display_name: "Plan Work"
  short_description: "Map lanes, critical paths, and scope"
  default_prompt: "Use $plan-work to map parallel lanes, the critical path, integration ownership, and verifiable scope."
policy:
  allow_implicit_invocation: false
